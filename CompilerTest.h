#pragma once
#ifdef _DEBUG
#include <Windows.h>

/*struct PINST_ARRAY;

void DataSectionTest(BYTE* Data, int PrintCount)
{
	for(int i = 0 ; i < PrintCount ; i += 2)
	{
		short Identifier;
		memcpy(&Identifier, &Data[i], sizeof(short));
		printf("%i\n", Identifier);
	}
}*/

#endif //_DEBUG_